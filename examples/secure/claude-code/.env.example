# Anthropic API key — passed directly to the broker (never reaches the agent container)
ANTHROPIC_API_KEY=sk-ant-...

# FAL.ai API key — passed directly to the broker (never reaches the agent container)
FAL_KEY=fal-...
